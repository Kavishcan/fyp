# Evaluation

- `instrument.py` — logs every routing decision, bytes, latency, audit events
- `metrics.py` — routing, quality, privacy, robustness, cost
- `sweep.py` — the sigma and m grid
- `ablation.py` — the seven-rung baseline ladder
- `reproduce.py` — baseline provenance, commands, versions and raw outputs

Build instrumentation in month 2. Without it, month 6 means rerunning everything.
Log coarse recall separately from final recall.
