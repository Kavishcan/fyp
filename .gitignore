__pycache__/
*.py[cod]
.Python
.venv/
venv/
env/
.ipynb_checkpoints/
.pytest_cache/

.DS_Store
.idea/
.vscode/

data/raw/
data/processed/
# Derived from real BEIR corpora in backend/vendor/beir/ (also gitignored) —
# regenerate with data/prepare_beir_nodes.py, don't commit.
data/mcp_nodes/
data/mcp_nodes_extra/
data/eval_results/
*.faiss
*.index
*.npy
*.pkl

experiments/runs/
experiments/logs/
# Reproducible pilot artifacts (embeddings, raw decisions and partitions).
# The measured summary and command are documented in docs/14-smart-router-pilot.md.
experiments/smart-pilot-scifact-30-v1/
experiments/metadata-pilot-scifact-30-*/
# Routing study raw artifacts; readable measurements are in docs/17.
experiments/routing-study-*/
*.log

.env
*.key

# Vendored third-party research repos (docs/10-baseline-selection.md).
# Not committed as part of this repo — cloned locally per the setup
# instructions in each adapter's docstring, pinned to a recorded commit.
backend/vendor/

# Frontend
frontend/node_modules/
frontend/.next/
frontend/out/
frontend/.env*.local
